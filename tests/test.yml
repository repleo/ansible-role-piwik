---
- hosts: localhost
  remote_user: root
  roles:
    - { role: ansible-role-piwik,
         piwik_db_user: piwik,
         piwik_db_password: P4SSword,
         piwik_db_host: localhost,
         piwik_db_name: piwikdb,
    }
